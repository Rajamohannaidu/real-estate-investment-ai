# Core ML/DL (Upgraded for Python 3.12)
# numpy must be >= 1.26 for Py 3.12
numpy==1.26.4
# tensorflow must be >= 2.16 for Py 3.12
tensorflow==2.16.1
pandas==2.2.2
scikit-learn==1.5.0
xgboost==2.0.3
lightgbm==4.3.0

# Explainable AI (Latest compatible)
shap==0.45.1
lime==0.2.0.1

# LangChain (Pinned to older versions compatible with Pydantic v1)
langchain==0.1.0
langchain-groq==0.0.1
langchain-community==0.0.13
python-dotenv==1.0.0

# Streamlit & Visualization (Latest compatible)
streamlit==1.35.0
plotly==5.22.0
matplotlib==3.8.4
seaborn==0.12.2

# Data Processing (Latest compatible)
joblib==1.4.2
scipy==1.13.1

# API & Environment
requests==2.31.0

# --- CRITICAL PINS for Compatibility ---
# This is the key. Tensorflow 2.16 requires pydantic<2.
# Langchain 0.1.0 is compatible with pydantic v1.
pydantic==1.10.13

# THIS IS THE FIX:
# Updated to a version compatible with langchain-groq (>=4.7) 
# AND streamlit (<5.0)
typing-extensions==4.11.0